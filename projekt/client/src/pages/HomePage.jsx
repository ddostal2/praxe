/**
 * Úvodní stránka aplikace.
 * @component
 * @returns {JSX.Element}
 */
const HomePage = () => {
  return (
    <section>
      <h1>Vítejte v eshopu</h1>
      <p>
        Základ frontendu běží na Vite + React + react-router-dom. Další stránky a
        volání API přidáte do složek <code>pages/</code> a <code>services/</code>.
      </p>
    </section>
  );
};

export default HomePage;
