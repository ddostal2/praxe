/**
 * Stránka O nás — ukázka druhé routy.
 * @component
 * @returns {JSX.Element}
 */
const AboutPage = () => {
  return (
    <section>
      <h1>O nás</h1>
      <p>Školní eshop — základ projektu podle pravidel v .cursor/rules.</p>
    </section>
  );
};

export default AboutPage;
